<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="4dp"
    android:paddingEnd="8dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Summary row (always visible) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Chevron: INVISIBLE when only 1 step, VISIBLE + clickable when >1 -->
        <TextView
            android:id="@+id/history_chevron"
            android:layout_width="28dp"
            android:layout_height="wrap_content"
            android:text="▶"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            android:gravity="center"
            android:padding="4dp" />

        <TextView
            android:id="@+id/history_expression"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_secondary"
            android:textSize="16sp"
            android:singleLine="true"
            android:ellipsize="start"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" = "
            android:textColor="@color/text_dim"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/history_result"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/result_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:id="@+id/history_timestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_ghost"
            android:textSize="11sp"
            android:paddingStart="8dp" />

    </LinearLayout>

    <!-- Expandable prior steps (shown when chevron tapped, only when group has >1 step) -->
    <LinearLayout
        android:id="@+id/history_steps_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingTop="2dp"
        android:visibility="gone" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider_subtle"
        android:layout_marginTop="4dp" />

</LinearLayout>
